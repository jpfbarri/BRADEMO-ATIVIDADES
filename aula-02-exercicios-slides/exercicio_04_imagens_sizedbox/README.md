# Exercício 4 - Imagens e SizedBox

Duas imagens de tamanhos diferentes separadas por um SizedBox. As imagens
ficam em `images/` e são declaradas em `assets:` no `pubspec.yaml`.

**Widgets estudados:** Image, SizedBox, FittedBox

## Como executar

```bash
flutter pub get
flutter run -d chrome
```
